version: '3'
services:
    maptomethod:
      build: .
      container_name: maptomethod
      #command: bash -c "gunicorn -c /src/gunicorn_conf.py wsgi:app -b 0.0.0.0:5000"
      #volumes:
      #  - ./:/src
      ports:
         - 5002:5000
      restart: always
      networks:
        maptomethod_net:
networks:
  maptomethod_net:
    driver: bridge
